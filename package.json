{
  "name": "blowincoldac-tests",
  "version": "1.0.0",
  "description": "Playwright tests for Blowin Cold AC website",
  "scripts": {
    "start": "node server.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "dependencies": {
    "express": "^5.2.1",
    "gm-review-scraper": "^1.1.0"
  }
}
